
const STREAMLINE_API="https://api.streamlinebusinessos.com";
document.addEventListener("DOMContentLoaded",()=>{
  const menu=document.querySelector(".menu-btn"),mobile=document.querySelector(".mobile-menu");
  if(menu&&mobile)menu.addEventListener("click",()=>{const open=mobile.classList.toggle("open");menu.setAttribute("aria-expanded",String(open));});
  const active=document.body.dataset.page; if(active)document.querySelector(`[data-nav="${active}"]`)?.classList.add("active");
  const reduced=matchMedia("(prefers-reduced-motion: reduce)").matches;
  if(!reduced){
    const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add("visible");observer.unobserve(e.target)}}),{threshold:.12,rootMargin:"0px 0px -40px"});
    document.querySelectorAll(".reveal").forEach((el,i)=>{el.style.transitionDelay=`${Math.min((i%4)*55,165)}ms`;observer.observe(el)});
    let ticking=false;window.addEventListener("pointermove",e=>{if(ticking)return;ticking=true;requestAnimationFrame(()=>{document.querySelectorAll("[data-depth]").forEach(el=>{const d=Number(el.dataset.depth||0);const x=(e.clientX-innerWidth/2)*d;const y=(e.clientY-innerHeight/2)*d;el.style.transform=`translate3d(${x}px,${y}px,0)`});ticking=false})},{passive:true});
  } else document.querySelectorAll(".reveal").forEach(el=>el.classList.add("visible"));
  const params=new URLSearchParams(location.search),product=params.get("product");if(product){const select=document.querySelector('select[name="product"]');if(select){const wanted=product.replaceAll("-"," ");const option=[...select.options].find(o=>o.textContent.toLowerCase().includes(wanted.toLowerCase())||o.value===product);if(option)select.value=option.value;}}
  const phone=document.querySelector('[data-consultation-form] input[name="phone"]');if(phone)phone.addEventListener("input",()=>{const d=phone.value.replace(/\D/g,"").slice(0,10);phone.value=d.length>6?`(${d.slice(0,3)}) ${d.slice(3,6)}-${d.slice(6)}`:d.length>3?`(${d.slice(0,3)}) ${d.slice(3)}`:d.length?`(${d}`:"";});
  document.querySelectorAll("form[data-consultation-form]").forEach(form=>form.addEventListener("submit",async e=>{e.preventDefault();const status=form.querySelector("[data-form-status]"),button=form.querySelector("[data-consult-submit]");if(!form.reportValidity())return;const f=new FormData(form);const body={name:String(f.get("name")||"").trim(),business:String(f.get("business")||"").trim(),email:String(f.get("email")||"").trim(),phone:String(f.get("phone")||"").trim(),products:[String(f.get("product")||"").trim()].filter(Boolean),source:"Website",message:String(f.get("message")||"").trim(),page:location.href,submittedAt:new Date().toISOString(),website:String(f.get("website")||"")};button.disabled=true;button.textContent="Sending…";status.className="form-note";status.textContent="Sending securely…";try{const r=await fetch(`${STREAMLINE_API}/api/v1/public/consultations`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});let data={};try{data=await r.json()}catch{}if(!r.ok)throw new Error(data?.error?.message||data?.message||`Request failed (${r.status})`);form.reset();status.className="form-note form-success";status.textContent="Request received. Streamline Systems will follow up to continue the conversation."}catch(err){status.className="form-note form-error";status.textContent=err?.message||"We could not send the request. Please try again."}finally{button.disabled=false;button.textContent="Start the conversation";}}));
});
