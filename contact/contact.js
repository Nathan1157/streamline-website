(()=>{const API='https://api.streamlinebusinessos.com/api/v1/public/consultations';const form=document.getElementById('consult-form'),status=document.getElementById('form-status');if(!form)return;form.addEventListener('submit',async e=>{e.preventDefault();status.className='form-status';status.textContent='Sending…';const fd=new FormData(form);const product=String(fd.get('product')||'').trim();const body={name:String(fd.get('name')||'').trim(),business:String(fd.get('business')||'').trim(),email:String(fd.get('email')||'').trim(),phone:String(fd.get('phone')||'').trim(),products:product?[product]:[],source:'website-contact',message:String(fd.get('message')||'').trim(),page:location.href,submittedAt:new Date().toISOString(),website:String(fd.get('website')||'')};try{const r=await fetch(API,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const j=await r.json().catch(()=>({}));if(!r.ok||!j.ok)throw new Error(j?.error?.message||'Unable to send request.');status.className='form-status success';status.textContent='Request received. Streamline will follow up with you.';form.reset()}catch(err){status.className='form-status error';status.textContent=err.message||'Unable to send request. Please call or email Streamline.'}})})();