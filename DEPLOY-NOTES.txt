STREAMLINE WEBSITE DEPLOYMENT NOTES

Clean routes:
  /software
  /pos
  /reserve
  /pizza
  /operations
  /training
  /workforce
  /proposals
  /display
  /inventory
  /contact
  /support
  /careers
  /admin

Admin:
  /admin is direct-link only. It is intentionally not linked from the public site.

404:
  404.html is included at the site root. Configure the static host to use it for missing pages.

Backend:
  The uploaded archive used for this build is a static front-end bundle only (HTML/CSS/JS).
  No server/API/backend source was present, so contact/support/admin backend actions have NOT been fabricated.
  They are ready to be wired when the actual production backend files are supplied.

V7 REAL WEB HQ
- /admin now uses the production Streamline Cloud HQ authentication endpoint.
- Sign-in endpoint: /api/v1/hq-auth/login
- Session validation: /api/v1/hq-auth/me
- Temporary password change flow is supported.
- HQ access token is held in sessionStorage and sent as a Bearer token to /api/v1/hq/*.
- The Cloud API already allows https://streamlinebusinessos.com and https://www.streamlinebusinessos.com via CORS.
- Web HQ loads real Cloud data for dashboard, leads, sold jobs, payroll, complaints, employees, scheduling, price approvals, notifications, and analytics.
- Individual panels gracefully show permission errors when the signed-in HQ account lacks the required permission.
- /admin remains intentionally unlinked from the public site.


V8 STAFF WEB HQ
- /admin is now a staff portal built from the real HQ + Cloud API, with zero demo rows.
- Any active HQ credential can sign in using the existing HQ Login ID/password.
- Staff see only today's lead/schedule view from /api/v1/hq/web-dashboard; this portal intentionally does not expose the full lead queue.
- Any staff member can log a customer complaint and report an internal issue.
- Owner 001 / owner role gets an additional Tax Receipts tool.
- Owner receipt uploads require Cloud API v2.14.32 included separately; receipt files persist under /opt/streamline/data/tax-receipts.
- /admin remains unlinked from the public website.
